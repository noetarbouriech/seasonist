# Find an offer by id
query findOfferById($offerId: String!) {
    findById(id: $offerId) {
        id
        title
        authorId
        jobCategoryIds
        companyId
        body
        open
        createdAt
        updatedAt
    }
}
