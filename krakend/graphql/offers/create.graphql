# Create an offer
mutation addOffer($title: String!, $body: String!, $jobCategoryIds: [String]!, $benefits: [String], $companyId: String!, $open: Boolean!, $authorId: String!) {
    addOffer(title: $title, body: $body, jobCategoryIds: $jobCategoryIds, benefits: $benefits, companyId: $companyId, open: $open, authorId: $authorId) {
        id
        title
        authorId
        jobCategoryIds
        benefits
        companyId
        body
        open
        createdAt
        updatedAt
    }
}
